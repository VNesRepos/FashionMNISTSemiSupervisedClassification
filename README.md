Neural network implemented in Python to evaluate classification performance on Fashion-MNIST using labeled data and semi-supervised learning.

Uses data augmentation and unlabeled samples to regularize training, preventing the network from memorizing small labeled sets and improving classification accuracy.

Details in the report.
